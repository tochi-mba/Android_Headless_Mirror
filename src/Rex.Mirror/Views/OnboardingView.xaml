<UserControl x:Class="Rex.Mirror.Views.OnboardingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource Ink}">
  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel MaxWidth="560" Margin="32,40" HorizontalAlignment="Center">
      <TextBlock Text="REX · FIRST RUN" FontFamily="{StaticResource Mono}" FontSize="11" Foreground="{StaticResource Signal}" />
      <TextBlock Text="Your phone, on this screen." FontSize="30" FontWeight="SemiBold" Margin="0,8,0,6" />
      <TextBlock Style="{StaticResource MutedText}" FontSize="14"
                 Text="One download and one tap on the phone. After that, plugging the phone in opens it here automatically." />

      <Border Margin="0,28,0,0" Background="{StaticResource Panel}" BorderBrush="{StaticResource Line}" BorderThickness="1" CornerRadius="12" Padding="20">
        <StackPanel>
          <StackPanel Orientation="Horizontal">
            <Border Width="26" Height="26" CornerRadius="13" Background="{StaticResource Signal}" VerticalAlignment="Top">
              <TextBlock Text="1" Foreground="{StaticResource Ink}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel Margin="14,0,0,0" Width="440">
              <TextBlock Text="Install scrcpy" FontSize="16" FontWeight="SemiBold" />
              <TextBlock Style="{StaticResource MutedText}" Margin="0,3,0,0"
                         Text="The official open-source mirroring engine from Genymobile. It is downloaded from GitHub and its SHA-256 checksum is verified before anything is installed." />
              <Grid Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button x:Name="InstallButton" Content="Install scrcpy" Style="{StaticResource PrimaryButton}" Click="OnInstall" />
                <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                  <ProgressBar x:Name="Progress" Height="6" Minimum="0" Maximum="1" Visibility="Collapsed" Foreground="{StaticResource Signal}" Background="{StaticResource Line}" BorderThickness="0" />
                  <TextBlock x:Name="InstallStatus" Style="{StaticResource MutedText}" FontSize="11" Margin="0,4,0,0" />
                </StackPanel>
              </Grid>
            </StackPanel>
          </StackPanel>

          <Rectangle Height="1" Fill="{StaticResource Line}" Margin="0,18" />

          <StackPanel Orientation="Horizontal">
            <Border Width="26" Height="26" CornerRadius="13" Background="{StaticResource Raised}" BorderBrush="{StaticResource Line}" BorderThickness="1" VerticalAlignment="Top">
              <TextBlock Text="2" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel Margin="14,0,0,0" Width="440">
              <TextBlock Text="Turn on USB debugging" FontSize="16" FontWeight="SemiBold" />
              <TextBlock Style="{StaticResource MutedText}" Margin="0,3,0,0"
                         Text="On the phone: Settings → About phone → tap Build number seven times. Then Settings → Developer options → USB debugging." />
            </StackPanel>
          </StackPanel>

          <Rectangle Height="1" Fill="{StaticResource Line}" Margin="0,18" />

          <StackPanel Orientation="Horizontal">
            <Border Width="26" Height="26" CornerRadius="13" Background="{StaticResource Raised}" BorderBrush="{StaticResource Line}" BorderThickness="1" VerticalAlignment="Top">
              <TextBlock Text="3" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel Margin="14,0,0,0" Width="440">
              <TextBlock Text="Plug in and tap Allow" FontSize="16" FontWeight="SemiBold" />
              <TextBlock Style="{StaticResource MutedText}" Margin="0,3,0,0"
                         Text="Connect with USB. The phone asks “Allow USB debugging?” once: tick “Always allow from this computer” and tap Allow. The mirror opens here by itself." />
            </StackPanel>
          </StackPanel>
        </StackPanel>
      </Border>

      <HeaderedContentControl Style="{StaticResource SettingRow}" Margin="0,18,0,0" Header="Start with Windows and wait for the phone in the tray">
        <CheckBox x:Name="StartWithWindows" Click="OnStartWithWindows" />
      </HeaderedContentControl>
      <TextBlock Style="{StaticResource MutedText}" FontSize="11" Margin="0,10,0,0"
                 Text="Nothing needs administrator rights. Everything stays on this PC." />
    </StackPanel>
  </ScrollViewer>
</UserControl>
