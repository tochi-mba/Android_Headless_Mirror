<UserControl x:Class="Rex.Mirror.Views.AdvancedSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel>
    <TextBlock Text="Android settings provider" Style="{StaticResource MutedText}" />
    <TextBlock Style="{StaticResource MutedText}" FontSize="11" Margin="0,2,0,8"
               Text="Live keys from this phone. Keys that could cut off ADB are protected; others may still be refused by Android." />
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <ComboBox x:Name="Namespace" MinWidth="90" SelectedIndex="0" SelectionChanged="OnNamespace">
        <ComboBoxItem Content="system" Tag="system" />
        <ComboBoxItem Content="secure" Tag="secure" />
        <ComboBoxItem Content="global" Tag="global" />
      </ComboBox>
      <TextBox x:Name="Search" Grid.Column="1" Margin="6,0" TextChanged="OnSearch" ToolTip="Filter keys and values" />
      <Button Grid.Column="2" Style="{StaticResource IconButton}" Content="{StaticResource IconRefresh}" ToolTip="Reload" Click="OnReload" />
    </Grid>
    <Border Margin="0,8,0,0" BorderBrush="{StaticResource Line}" BorderThickness="1" CornerRadius="8" Background="{StaticResource Ink}">
      <ListBox x:Name="Rows" MaxHeight="260" Background="Transparent" BorderThickness="0" SelectionChanged="OnRowSelected"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled">
        <ListBox.ItemContainerStyle>
          <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="Foreground" Value="{StaticResource Text}" />
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                  <Border x:Name="Border" Background="Transparent" Padding="{TemplateBinding Padding}">
                    <ContentPresenter />
                  </Border>
                  <ControlTemplate.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                      <Setter TargetName="Border" Property="Background" Value="{StaticResource Raised}" />
                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="True">
                      <Setter TargetName="Border" Property="Background" Value="{StaticResource Hover}" />
                    </Trigger>
                  </ControlTemplate.Triggers>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <StackPanel>
                <TextBlock Text="{Binding Key}" FontFamily="{StaticResource Mono}" FontSize="11" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                <TextBlock Text="{Binding Value}" Style="{StaticResource MutedText}" FontSize="11" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
              </StackPanel>
              <TextBlock Grid.Column="1" Text="{Binding Risk}" FontSize="10" Foreground="{StaticResource Muted}" VerticalAlignment="Center" Margin="8,0,0,0" />
            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Border>
    <Grid Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBox x:Name="Key" ToolTip="Key" />
      <TextBox x:Name="Value" Grid.Column="1" Margin="6,0" ToolTip="New value" />
      <Button Grid.Column="2" Content="Write" Style="{StaticResource PrimaryButton}" Click="OnWrite" />
      <Button Grid.Column="3" Content="Delete" Style="{StaticResource DangerButton}" Margin="6,0,0,0" Click="OnDelete" />
    </Grid>
    <TextBlock x:Name="Status" Style="{StaticResource MutedText}" Margin="0,6,0,0" />
  </StackPanel>
</UserControl>
